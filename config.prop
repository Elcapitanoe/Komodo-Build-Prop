# You can set those manually if you don't have phisical volume keys.
#
# On sensitive key, values when true are basically meant,
# to set and use the sensitive checks, else when false will ignore the check.

pixelprops.sensitive.device=false
pixelprops.sensitive.security_patch=false
pixelprops.sensitive.soc=false
pixelprops.sensitive.sdk=false
pixelprops.sensitive.props=false
pixelprops.sensitive.pihooks=false